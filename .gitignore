# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Secrets locais: .env já foi versionado historicamente (contém só valores
# não sensíveis hoje), mas issue #24 passou a instruir colocar
# SUPABASE_SERVICE_ROLE_KEY/MS_TENANT_ID/MS_CLIENT_ID em variáveis de
# ambiente locais — segredo novo não pode ir para um arquivo rastreado.
# Novo padrão: segredos ficam em .env.local (Vite já dá precedência a ele
# sobre .env automaticamente), nunca em .env.
.env

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Superpowers brainstorming companion
.superpowers/

# Git worktrees used for isolated implementation work
.worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cache de fotos da roleta de retrospectiva: ~1,3 MB de foto + e-mail de ~20
# colegas embutidos como código. Dado pessoal, não código — e git não esquece,
# mesmo se o arquivo for apagado depois. Gerado manualmente, fora do repositório.
src/lib/retrospectivas/photos-cache.ts
