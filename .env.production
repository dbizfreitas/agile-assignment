# Overrides para build em modo production (`vite build` sem --mode).
#
# Este arquivo NAO e suficiente sozinho: o Vite le .env + .env.<mode>, entao
# um build em modo development (`vite build --mode development`, `vite dev`)
# ignora este arquivo por completo. As variaveis que sustentam TODOS os modos
# estao em .env, versionado de proposito -- ver o comentario la.
#
# Mesma regra do .env: somente valores publicos, nunca segredos.

VITE_SUPABASE_PROJECT_ID="nuvrdppxecbowxopbqcr"
VITE_SUPABASE_URL="https://nuvrdppxecbowxopbqcr.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_SViAZxkVWQMnbUCzssZaPw_NSP6v03K"
